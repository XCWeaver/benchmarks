appConfig: weaver.toml
repo: docker.io/tiagomalhadas

ScalingSpec:
  minReplicas: 1
  maxReplicas: 1

listeners:
  - name: postnot
    public: true
    port: 12345

resourceSpec:
  requests:
    memory: "4Gi"
    cpu: "1"  
  limits:
    memory: "4Gi"
    cpu: "1"  